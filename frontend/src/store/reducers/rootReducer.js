
const initialState = ({
	selectedItem: '',
	regions: [],
	selectedRegion: null,
	listOpen: false,
	startDate: null,
	endDate: null
})

export function reduce(state = initialState, action) {
	switch (action.type) {
		default:
			return state
	}
}
